rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full read/write for now (open access for SmartUPLI)
    // TODO: Tighten rules after authentication is implemented
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
